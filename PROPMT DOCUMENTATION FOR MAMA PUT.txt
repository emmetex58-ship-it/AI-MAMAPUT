PROMPT DOCUMENTATION FOR MAMA PUT ONLINE.


Act as an expert front-end developer and UI/UX engineer. Design and write a complete, production-ready landing page for a premium local food delivery platform named "MamaPutOnline". The project must consist of clean, semantic HTML5 markup and a responsive, highly modular Vanilla CSS3 stylesheet. 

Apply the following precise structural layout architectural rules and design system specs:

 1. Visual Theme & Color Palette (CSS Variables)
 Canvas Background: A deep dark obsidian theme (#0b0f19) for ultra-premium text contrast.
 Card Surfaces: Elevated card surfaces (#121826) with crisp, dark grid dividers (#222b3e).
 Primary Brand Color: Rich, appetizing deep Nigerian burgundy (#7a0c02).
 Accent / CTA Color: High-contrast warm gold (#e5a93c) used exclusively for active states, pricing highlights, and calls to action.

 2. Header & Navigation Architecture
The header must be completely sticky (fixed to the top of the viewport on scroll) with a subtle drop shadow.
The navigation menu must feature 5 explicit links: " Home”,” Products", "Services", "Customer Service", and "Contact Us".
Configure the navigation layout using an ID anchor hash routing framework (e.g., href="#products", href="#contact").
 Implement globally smooth scrolling transitions with CSS (`scroll-behavior: smooth`) and appropriate `scroll-padding-top` to prevent the sticky header from blocking text elements when a link is clicked.
 Include a sleek, functional expanding search input bar built into the navigation frame.

 3. Page Structure & Components
Hero Section: Minimum 30-35vh viewport partition featuring an asymmetrical split-column layout. The left column holds bold marketing typography, body copy, and a premium gold CTA button. The right column embeds a high-quality food background imagery asset.
Sticky Layout Split: Inside the main element, implement a two-column setup. 
  Left column: A fixed category vertical sidebar navigation map ("Cooked Soups", "Rice & Carbs", "Sides & Proteins").
   Right column: The core scrollable product workspace displaying food grids.
 Product Grids: Display responsive cards using modern CSS Grid with a minimum width profile of 220px per item. Cards must contain promotional tags (e.g., "10% OFF", "HOT SELLER"), structured image wraps, price nodes, and interactive "+ Add" button triggers with explicit hover states.
 Info Rows: Dedicate clean rows explicitly mapped to the `#services` and `#customer-service` anchor IDs to break up information cleanly.

 Dedicated Contact Us & Support Grid
Create a comprehensive interactive "Contact Us" grid section mapped precisely to `#contact`.
 Left Side Component: A robust structural support ticket contact card holding input text fields for Full Name, Email address, a dropdown Selector component for assistance type (e.g., Delivery Delay, Corporate Catering, Bulk Order), a clean Textarea field for message details, and a styled submit button.
Right Side Component: An operational company metadata card cleanly presenting the kitchen HQ address, highlighted bold phone numbers, and operational calendar hours.

 Sticky Footer Matrix
 A sleek, minimalist base footer container featuring social media quick-links and clear copyright metadata.

Ensure all layout components scale down elegantly onto smaller screens using robust responsive CSS Media Queries that collapse the multi-column sections into vertical block streams on mobile displays. Provide the HTML and CSS blocks separately with extensive inline documentation outlining the system choices.

